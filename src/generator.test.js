import {describe,expect,it} from 'vitest';import {createArduinoGenerator,pythonToArduino} from './generator.js';
describe('Python to Arduino generator',()=>{it('translates the documented robot dialect',()=>{const output=pythonToArduino('from arduino import *\ndef setup():\n pin_mode(13, OUTPUT)\ndef loop():\n digital_write(13, HIGH)\n sleep(0.5)');expect(output).toContain('pinMode(13, OUTPUT);');expect(output).toContain('digitalWrite(13, HIGH);');expect(output).toContain('delay(500);')});it('reports unsupported code with its line number',()=>expect(()=>pythonToArduino('dance()')).toThrow('Line 1'))});
describe('TurboWarp block generator',()=>{it('generates variables and hardware setup from a workspace',()=>{const variable={name:'motor speed'};const servo={type:'arduino_servo',getFieldValue:name=>({PIN:'9',ANGLE:'90'})[name],getNextBlock:()=>null};const hat={type:'arduino_start',getInputTargetBlock:()=>servo};const workspace={getAllVariables:()=>[variable],getTopBlocks:()=>[hat],getAllBlocks:()=>[hat,servo]};const output=createArduinoGenerator().workspaceToCode(workspace);expect(output).toContain('double motor_speed = 0;');expect(output).toContain('Servo servo_9;');expect(output).toContain('servo_9.attach(9);');expect(output).toContain('servo_9.write(constrain(90, 0, 180));')})});
